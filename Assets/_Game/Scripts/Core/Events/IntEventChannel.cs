using UnityEngine;

/// <summary>Int event channel — damage amounts, phase indices, etc.</summary>
[CreateAssetMenu(menuName = "ToolCrate/Events/Int Event Channel")]
public class IntEventChannel : EventChannel<int> { }